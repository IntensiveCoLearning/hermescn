- GitHub ID：176461498
- Name：MIA-Ether
- 时区：UTC+8
- 报名动机：我目前就读于AIx数据科学相关专业，长期关注 AI、AI Agent、数据分析以及 Web3 等方向，也在持续使用 Python、GitHub、AI 编程工具等进行学习和项目实践。
过去我更多是在“使用 AI”——让模型辅助写代码、整理信息、生成内容和提高学习效率。但现在我更加focus on如何把 AI 真正转化为一个能够理解任务、执行任务、产出结果并且可以复现的 Agent Workflow。Hermes 这次共学的路径“安装 → 对话 → 明确任务 → 生成产物 → 训练为生产级 Agent”与我目前的探索方向非常契合。
我希望通过这 7 天的高强度实践，系统理解 Hermes 的运行方式、任务拆解、Profile、Skills、安全机制以及故障恢复，并最终结合自己的实际场景完成一个真正可运行、可验收的个人项目。
同时，我也希望借助残酷共学的打卡和社区机制，认识更多正在探索 AI Agent 的开发者和实践者，在真实交流中不断修正自己的理解。
- 行业：准大一新生，就读于广州某重点高校 base大学城
自我探索/创新实践
人工智能（AI Agent）｜大数据与数据分析｜互联网｜Web3

## Notes

<!-- Content_START -->
<!-- DAILY_CHECKIN_2026-08-27_START -->
# 2026-08-27

# Hermes Agent 七天学习打卡｜Day 1

## 一、今日学习主题

**Day 1：安装并完成第一次对话（环境验证与 Doctor 诊断）**

## 二、今日学习目标

本日主要完成 Hermes Agent 的基础环境搭建和第一次使用，重点包括：

* 安装 Hermes Agent 及其运行依赖
* 配置本地运行环境
* 完成 Nous Portal 登录与模型配置
* 启动 Hermes Agent 并进行第一次对话
* 使用 `hermes doctor` 对运行环境进行诊断
* 了解 Hermes Agent 的基本运行结构

## 三、安装过程

本次使用 Windows PowerShell 完成 Hermes Agent 安装。

安装过程中，Hermes Installer 自动检测并配置了相关依赖，包括：

* Python 3.11.15
* Git 2.55.0
* Node.js 24.19.0
* uv 0.12.6
* ripgrep
* ffmpeg

安装器首先尝试通过 SSH 获取 Hermes 源码，由于 Windows 环境下 SSH Host Key Verification 未通过，随后自动切换 HTTPS。HTTPS 下载过程中又出现连接中断，但安装器进一步自动切换 ZIP 下载，最终成功获取并解压项目源码。

随后安装器成功创建 Python 虚拟环境，并通过 `uv.lock` 完成依赖安装。

最终核心安装结果：

```text
Hermes Agent v0.20.6
Python 3.11.15
OpenAI SDK 2.24.0
```

同时完成了 Hermes 配置目录、Skills、Session、Logs 等基础目录的初始化。

## 四、模型与账户配置

安装完成后进入 Hermes Setup Wizard。

最初 Nous Portal 设备授权出现超时，因此第一次登录未完成。之后通过：

```powershell
hermes model
```

重新进入模型配置，并选择 Nous Portal。

随后成功完成 OAuth 登录：

```text
Login successful!
Auth state: C:\Users\24525\AppData\Local\hermes\auth.json
```

这说明 Hermes 已经获得有效的模型 Provider 认证，可以继续进行 Agent 对话。

## 五、第一次对话

完成模型配置之后启动 Hermes，并进行第一次基础对话，验证 Agent 是否能够正常响应。

本次第一次对话主要用于确认：

1. Hermes CLI 可以正常启动；
2. 模型 Provider 已成功连接；
3. Agent 能够接收自然语言输入；
4. 会话状态能够正常保存。

Doctor 后续检查显示：

```text
state.db exists (1 sessions)
1 messages, 1 sessions
```

因此可以确认第一次对话已经被 Hermes 正常记录到本地 Session 数据中。

## 六、Doctor 环境诊断

使用以下命令进行完整诊断：

```powershell
hermes doctor
```

本次诊断主要检查了以下几个部分：

### Python Environment

```text
✓ Python 3.11.15
✓ SQLite 3.53.1
✓ Virtual environment active
✓ Version files consistent (0.20.6)
```

说明 Python、SQLite、虚拟环境以及 Hermes 版本状态均正常。

### SSL / CA Certificates

```text
✓ SSL CA certificate bundle is valid
```

说明网络安全证书环境正常。

### Required Packages

OpenAI SDK、Rich、python-dotenv、PyYAML、HTTPX 等核心依赖全部正常。

部分 Telegram、Discord 依赖属于可选组件，因此未安装不会影响当前基础使用。

### Configuration Files

```text
✓ .env file exists
✓ API key or custom endpoint configured
✓ config.yaml exists
✓ Config version up to date
✓ No deprecated config keys or env vars
```

说明 Hermes 的核心配置文件已经建立并且版本正常。

### Auth Providers

```text
✓ Nous Portal auth (logged in)
```

说明模型 Provider 已成功认证。

### Directory Structure

Hermes 的 cron、sessions、logs、skills、memories 等目录均正常建立，说明本地 Agent 工作目录已经初始化完成。

### External Tools

Git、ripgrep、Node.js 和 Agent Browser 等基础工具已经可以使用。

部分 Docker、Chromium 等能力未安装，属于额外工具，不影响当前基础 Agent 使用。

### Tool Availability

当前已经具备：

```text
✓ Terminal
✓ Skills
✓ Memory
✓ Task Planning
✓ File
✓ Code Execution
✓ Browser-use
✓ Computer-use
✓ TTS
✓ Vision
✓ Web Search
```

部分第三方平台功能因为没有配置对应凭据，因此处于关闭状态。

## 七、Doctor 最终结果

本次环境诊断最终输出：

```text
All checks passed! 🎉
```

因此可以确认：

**Hermes Agent 的基础运行环境已经完成配置，并通过 Doctor 验证。**

## 八、今日学习收获

通过 Day 1 的安装和诊断，我对 Hermes Agent 有了初步认识。

之前更容易把 Agent 理解成“一个可以聊天的 AI”，但实际安装和配置之后可以发现，一个可工作的 Agent 系统由多个部分组成：

```text
模型 Provider
      ↓
Agent Runtime
      ↓
Tools / Skills
      ↓
Memory / Session
      ↓
Terminal / Browser
      ↓
任务执行
      ↓
最终产物
```

因此，Agent 的核心并不只是模型本身，而是**模型、工具、运行环境和任务执行流程的组合**。

另外，`hermes doctor` 让我认识到 Agent 工具需要像开发环境一样进行系统性检查。出现某个功能未启用，并不一定代表安装失败，需要区分“核心运行环境问题”和“可选功能尚未配置”。

## 九、今日问题与解决

本次安装过程中主要遇到了三个问题：

### 1. Git SSH 无法连接

出现：

```text
Host key verification failed.
```

安装器自动切换 HTTPS，因此没有影响最终安装。

### 2. HTTPS Clone 中途断开

出现：

```text
RPC failed
early EOF
```

安装器自动切换 ZIP 下载，最终成功获取项目文件。

### 3. Nous Portal 首次授权超时

第一次设备授权没有在规定时间内完成，之后重新执行模型配置并完成 Portal OAuth 登录，最终解决。

这些问题让我认识到，在实际部署 Agent 时，网络、依赖、认证和工具链都可能成为影响运行的因素，需要根据日志定位，而不是简单反复安装。

## 十、Day 1 验收结果

本日任务完成情况：

| 项目                       | 状态 |
| :----------------------- | :- |
| Hermes Agent 安装          | ✅  |
| Python 环境验证              | ✅  |
| Git / Node.js / uv 等依赖验证 | ✅  |
| Hermes CLI 验证            | ✅  |
| Nous Portal 登录           | ✅  |
| 模型配置                     | ✅  |
| 第一次对话                    | ✅  |
| Doctor 诊断                | ✅  |
| 基础工具检查                   | ✅  |
| 本地 Session 建立            | ✅  |

### Day 1 最终结论

**已完成 Hermes Agent 的安装、首次配置、第一次对话和环境诊断，`hermes doctor`** **最终显示** **`All checks passed!`，Day 1 任务完成。**

下一阶段将从“环境搭建”进入“实际任务执行”，开始学习如何给 Hermes 一个明确目标，并让 Agent 通过工具完成能够验收的真实任务。
<!-- DAILY_CHECKIN_2026-08-27_END -->

<!-- DAILY_CHECKIN_2026-08-28_START -->
# 2026-08-28

# Day 2：熟悉 Hermes Agent 的基础使用

## 一、实验目标

本次实践主要用于熟悉 Hermes Agent 的基本操作流程，包括 Agent 启动、模型选择与切换、Provider 配置、基础对话以及运行环境诊断，为后续使用 Hermes Agent 调用工具、Skills 和执行自动化任务打下基础。

## 二、实验环境

* 操作系统：Windows
* Python：3.11.15
* Hermes Agent：v0.20.6
* Hermes 安装目录：`C:\Users\24525\AppData\Local\hermes\hermes-agent`
* 当前模型：GLM-5.2
* 模型提供商：Z.AI / GLM
* Hermes 工具数量：22
* Hermes Skills：73

## 三、实践过程

### 1. 启动 Hermes Agent

通过 PowerShell 执行：

```
hermes

```

成功进入 Hermes Agent 交互界面。

启动界面能够显示当前可用的 Tools、Skills、模型以及 Session 信息，说明 Hermes Agent 的基础运行环境已经正常。

### 2. 熟悉基础工具与 Skills

启动后可以看到 Hermes 当前提供了多个工具，包括：

* `browser`：浏览器自动化
* `browser-use`：浏览器执行
* `computer_use`：计算机桌面操作
* `terminal`：终端命令执行
* `file`：文件读取、搜索和修改
* `code_execution`：代码执行
* `cronjob`：定时任务
* `delegation`：任务委派
* `memory`：持久化记忆
* `skills`：Skills 管理
* `vision`：视觉分析
* `tts`：文本转语音
* `todo`：任务管理
* `session_search`：历史 Session 搜索

同时 Hermes 已经同步了大量 Skills，可用于软件开发、GitHub、研究、文件处理、生产力和自动化等场景。

### 3. 解决模型调用问题

最初使用 Nous Portal 中的部分付费模型时，出现了：

```
HTTP 404
Model 'openai/gpt-5.6-luna' requires available credits.
Your account balance is too low to use paid models.

```

经过排查发现，并不是 Hermes 安装失败，而是当前 Nous Portal 账户没有可用的付费模型额度。

因此没有继续使用付费模型，而是通过：

```
hermes model

```

重新配置模型 Provider。

### 4. 接入 Z.AI / GLM

在 Hermes 的模型配置界面中选择：

```
Z.AI / GLM

```

随后选择 Coding Plan Endpoint，并配置模型。

最终选择：

```
Provider：Z.AI / GLM
Model：glm-5.2

```

配置完成后，Hermes 成功切换到 GLM-5.2。

### 5. 进行实际对话测试

配置完成后进行了最基础的连通性测试：

```
你好，请只回复一句：GLM-5.2 已成功接入 Hermes。

```

Hermes 成功返回：

```
GLM-5.2 已成功接入 Hermes。

```

说明模型配置、Provider 连接以及 Hermes Agent 的基础推理链路均能够正常工作。

### 6. 使用 Doctor 进行环境诊断

完成模型配置后执行：

```
hermes doctor

```

最终检查结果显示：

```
✓ Python 3.11.15
✓ SQLite 3.53.1
✓ Virtual environment active
✓ Version files consistent
✓ Configuration Files
✓ Z.AI / GLM
✓ External Tools
✓ Tool Availability
✓ Built-in memory active

All checks passed! 🎉

```

其中 Z.AI / GLM API Connectivity 检查已经显示：

```
✓ Z.AI / GLM

```

证明新的模型 Provider 已经成功接入。

## 四、实践结果

本次实践完成了 Hermes Agent 从启动到实际模型调用的完整基础流程：

```
启动 Hermes
      ↓
查看 Tools / Skills
      ↓
进入模型配置
      ↓
解决 Nous Portal 付费模型额度问题
      ↓
接入 Z.AI / GLM
      ↓
选择 GLM-5.2
      ↓
实际对话测试
      ↓
hermes doctor
      ↓
All checks passed

```

当前 Hermes 已能够正常启动，并使用 GLM-5.2 进行对话。

## 五、问题与解决

本次实践中主要遇到两个问题。

**问题一：Nous Portal 登录及模型调用问题**

最初虽然能够完成 Nous Portal 登录，但调用 `openai/gpt-5.6-luna` 时提示账户没有可用 credits。

**解决方法：**

切换到其他 Provider，最终使用 Z.AI / GLM Coding Plan，并选择 GLM-5.2。

**问题二：部分工具仍处于不可用状态**

`hermes doctor` 中仍存在部分 Warning，例如 Playwright Chromium 尚未安装、Discord 未配置、GitHub Token 未配置等。

这些并不影响当前 Hermes 的基础对话功能。当前核心工具、Skills、Terminal、Memory、Computer Use 等已经能够正常加载。

## 六、实践总结

通过 Day 2 的实践，对 Hermes Agent 的基本架构和使用方式有了初步认识。

Hermes 并不是单纯的聊天机器人，而是由 **模型 Provider + Agent + Tools + Skills + Memory + Session** 等部分组成的 Agent 框架。模型负责推理，而 Tools 和 Skills 则进一步赋予 Agent 执行实际任务的能力。

本次已经完成模型接入和基础运行验证。下一阶段将重点从“能够对话”转向“能够执行任务”，进一步测试 Hermes 对文件、终端、浏览器、Skills 和自动化任务的调用能力。
<!-- DAILY_CHECKIN_2026-08-28_END -->
<!-- Content_END -->
