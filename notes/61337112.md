- GitHub ID：61337112
- Name：Thomas-YHS
- 时区：UTC+8
- 报名动机：之前也使用过Hermes不过并没有完全融入学习生产，希望通过这次共学能够真正的找到Hermes的优势和工程化的角度。同时，现在也在学习使用DSH，也希望探索一下有没有融合的可能性
- 行业：CEX

## Notes

<!-- Content_START -->
<!-- DAILY_CHECKIN_2026-08-27_START -->
# 2026-08-27

# HermesCN 残酷共学 · Day 1 打卡

## 今日主题
安装并完成第一次对话：环境验证与 `hermes doctor` 诊断。

我的 Hermes 环境此前已经安装完成，因此今天没有重复安装，而是按照 Day 1 验收目标做了一次完整的实时验证。

## 今天完成了什么

1. **验证运行环境**
   - Hermes Agent：v0.20.5
   - Python：3.11.16
   - 虚拟环境：已激活
   - SQLite：WAL 模式正常
   - 当前 Profile：`ai-web3-dev`
   - 当前模型：`gpt-5.6-sol`
   - OpenAI Codex OAuth：已登录

2. **运行环境诊断**
   - 执行了 `hermes doctor`
   - Python、SSL、核心依赖、配置目录、命令入口、数据库、Memory、Skills Hub 等核心检查通过
   - 内置 Memory 正常，Profile 可识别，常用工具可加载

3. **完成独立的第一次对话验收**
   - 通过 CLI 发起最小对话测试
   - 实际返回：`Hermes Day 1 OK`
   - 证明“启动 Hermes → 调用模型 → 获得响应”的最小链路正常

## 诊断中发现的问题

- Hermes 安装版本比上游落后 110 个 commits，后续需要评估更新
- Profile 配置版本为 v26，最新为 v39，需要执行配置迁移
- `display.tool_progress_overrides` 已弃用，需要迁移到 `display.platforms`
- Kimi 连通性检查返回 HTTP 500，但当前 OpenAI Codex 模型正常，不阻塞基础使用
- `computer_use`、`image_gen`、`x_search` 等可选能力依赖未满足，不影响 Day 1 验收

今天只做诊断，没有直接执行 `hermes doctor --fix`，避免在未检查变更范围前修改配置。

## 今日收获

`hermes doctor` 的价值不是“看到所有项目都变成绿色”，而是快速区分：

- 哪些问题会阻塞当前任务
- 哪些只是暂未配置的可选能力
- 哪些配置需要迁移，但应先确认修改范围

以后排查 Hermes 可以遵循：`version → status → doctor → 最小对话测试`。

## 明日计划

进入 Day 2，熟悉 Hermes 的基础使用：盘点 Tools 和 Skills，并让 Hermes 完成一次有明确输入、文件产物和验收结果的真实学习任务。
<!-- DAILY_CHECKIN_2026-08-27_END -->

<!-- DAILY_CHECKIN_2026-08-28_START -->
# 2026-08-28

# HermesCN 残酷共学 · Day 2 打卡

## 今日主题

熟悉 Hermes 的基础使用：配置管理、Profile 隔离、模型切换与结果验证。

## 今天完成了什么

### 1. 完成 Profile 配置迁移

对 `ai-web3-dev` Profile 执行配置迁移：

```bash
hermes --profile ai-web3-dev config migrate
```

实际结果：

```text
Config version: v26 → v39
```

迁移完成后，通过 `config check` 和 `profile show` 检查配置状态，确认新版本配置已经生效。

### 2. 检查配置迁移产生的变化

迁移器自动调整了部分默认配置，包括：

- 后台任务通知切换为简洁模式
- Subagent 工具调用预算提高
- 并行 Subagent 数量上限提高
- Profile 配置格式升级到最新版本
- Secret redaction 保持启用

同时确认以下内容没有因迁移丢失：

- `SOUL.md` 中的 Tim 学习助手定位
- AI × Web3 与 Go 后端转型目标
- 用户画像与持久 Memory
- 工作区路径与已有 Skills

### 3. 修正并验证模型配置

迁移过程中一度错误地把 Profile 模型设置成了 Kimi。发现后进行了纠正，并清理了遗留的 Kimi `base_url`。

当前配置已经验证为：

```yaml
model:
  default: gpt-5.6-sol
  provider: openai-codex
  base_url: ""
```

最终状态：

```text
Profile: ai-web3-dev
Model: gpt-5.6-sol (openai-codex)
Config version: 39
Skills: 379
SOUL.md: exists
```

## 今天的收获

1. **Profile 是独立运行环境**

   不同 Profile 可以分别维护模型、配置、Memory、Skills 和身份设定，修改时必须明确目标 Profile，不能只看当前 WebUI 使用的模型。

2. **配置迁移后必须重新验证**

   命令成功不代表最终配置一定符合预期。迁移后至少应检查：

   ```text
   config check
   profile show
   model provider
   base_url
   config version
   ```

3. **模型配置由多个字段共同决定**

   切换模型不能只修改模型名，还需要核对：

   - `model.default`
   - `model.provider`
   - `model.base_url`
   - 对应认证信息

   如果残留了其他 Provider 的 `base_url`，请求可能被发送到错误的服务。

4. **用户纠正也是重要的验证信号**

   今天发现模型配置错误后，没有继续基于错误假设操作，而是根据实际目标重新修改并回读配置。Agent 的执行结果仍然需要人工检查，尤其是模型、权限和外部写入等关键配置。

## 当前结果

今天完成了一个完整的 Hermes 配置管理闭环：

```text
发现旧配置
→ 执行迁移
→ 检查迁移结果
→ 发现模型错误
→ 修正 Provider 与模型
→ 清理遗留配置
→ 回读验证最终状态
```

## 明日计划

进入 Day 3，把一个模糊学习需求改写成 Hermes 可以执行和验收的任务。

计划选择一个 Go × Web3 后端学习场景，明确：

- 输入材料
- 执行步骤
- 文件产物
- 验收命令
- Proof-of-Work
<!-- DAILY_CHECKIN_2026-08-28_END -->
<!-- Content_END -->
